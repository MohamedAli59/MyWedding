{% extends 'admin.html.twig' %}

{% block title %}Adresses{% endblock %}

{% block body %}
    <h1>Adresses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adress.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ adress.Numero }}</td>
            </tr>
            <tr>
                <th>Voie</th>
                <td>{{ adress.Voie }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ adress.Ville }}</td>
            </tr>
            <tr>
                <th>Code_Postal</th>
                <td>{{ adress.CodePostal }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ adress.Pays }}</td>
            </tr>
            <tr>
                <th>Date_add</th>
                <td>{{ adress.dateAdd ? adress.dateAdd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_update</th>
                <td>{{ adress.dateUpdate ? adress.dateUpdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_delete</th>
                <td>{{ adress.dateDelete ? adress.dateDelete|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adresses_index') }}">back to list</a>

    <a href="{{ path('adresses_edit', {'id': adress.id}) }}">edit</a>

    {{ include('adresses/_delete_form.html.twig') }}
{% endblock %}
