<form method="post" action="{{ path('mariages_delete', {'id': mariage.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mariage.id) }}">
    <button class="btn btn-danger text-center">Supprimer</button>
</form>
