{% extends 'admin.html.twig' %}

{% block title %}Prestations{% endblock %}

{% block container %}

    <div class="card-body text-center col-lg-5 mt-3 mb-5 mx-auto" id="container">
        <h1 class="mb-4 bienvenue ">Voir Prestation</h1>
        <div class="card mt-5 col-10 col-sm-10 col-md-6 col-lg-12 col-xl-12 mx-auto cardInfos">
            <div class="mt-4">
                <span class="col-12 font-weight-bold">Nom :</span>
            </div>
            <div class="mt-3">
                <span class="border p-1">{{ prestation.Nom }}</span>
            </div>
            <div class="card-body mt-4 mb-3">
                <span class=" font-weight-bold">Description :</span>
                <p class="card-text pt-3">{{ prestation.Description }}</p>
            </div>
        </div>

        <div class="btn-group btn-group-sm mt-3 mb-5 actionMobile" role="group" aria-label="...">
            <a href="{{ path('prestations_edit', {'id': prestation.id}) }}" type="button"
               class="btn btn-info pt-2">Modiffier</a>
            {{ include('prestations/_delete_form.html.twig') }}
        </div>

    </div>

{% endblock %}



