<form method="post" action="{{ path('prestations_mariages_delete', {'id': prestations_mariage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestations_mariage.id) }}">
    <button class="btn">Delete</button>
</form>
